import { LandingtimerPipe } from './landingtimer.pipe';

describe('LandingtimerPipe', () => {
  it('create an instance', () => {
    const pipe = new LandingtimerPipe();
    expect(pipe).toBeTruthy();
  });
});
